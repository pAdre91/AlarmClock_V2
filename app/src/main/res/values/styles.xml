<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--Number picker-->
    <style name="DefaultNumberPickerTheme" parent="ThemeOverlay.AppCompat.Dark">
        <item name="colorControlNormal">@android:color/transparent</item>
        <item name="android:textColorPrimary">@color/purple_350</item>
        <item name="android:textSize">32sp</item>
    </style>

    <style name="SwitchTheme" parent="Theme.AppCompat.Light.NoActionBar">
        ...
        <!-- Active thumb color & Active track color(30% transparency) -->
        <item name="colorControlActivated">@color/purple_200_alpha</item>
        <!-- Inactive thumb color -->
        <item name="colorSwitchThumbNormal">@color/white</item>
        <!-- Inactive track color(30% transparency) -->
        <item name="android:colorForeground">@color/gray_medium</item>
        ...
    </style>
</resources>